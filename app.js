const selectedSeatEl = document.getElementById("selected-seat");

// handle selection seat
function handSelectSeat(event) {
  selectedSeatEl.innerHTML = 
  `<li class="flex justify-between">
        <span>${even.innerte}</span>
        <span></span>
        <span></span>
    </li>`;
}
